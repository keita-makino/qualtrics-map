// import { useEffect } from 'react';
// import { useTrackedState, useUpdate } from '../store';

// const useMapboxClient = () => {
//   const state = useTrackedState();
//   const update = useUpdate();

//   useEffect(() => {
//     if (!state.mapboxClient) return;
//     if (state.inputs.length === 0) return;
//   }, [state.mapboxClient]);
// };
